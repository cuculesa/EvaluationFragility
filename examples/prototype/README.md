# Original prototype snapshot

These files are the unmodified inputs supplied for the production-hardening pass. They are retained only for audit and migration testing. Do not deploy them directly.

The deployable package lives under `src/evalfrag/`; the legacy summary is migrated into `examples/synthetic/results.json` for dashboard regression testing.
